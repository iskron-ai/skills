---
name: entry
description: "Используй ПЕРЕД ответом на всё, что касается методологии, NKS или графа пользователя: вимарша, крия, холон, карта, феномен, бяньхуа, эстафета; «в графе», «по моим заметкам», «мы обсуждали», «что говорит реалм», незнакомый термин из словаря пользователя. Use BEFORE answering anything touching the user's NKS graph or methodology: vimarsha, kriya, holon, karta, phenomenon, bianhua, \"the graph\", \"my notes\", \"we discussed\", \"what does the realm say\". Сомневаешься — входи: ориентация дешева, ответ из training data по теме, которую реалм проработал, дорог и часто неверен."
---

# Вход в методологию

Пользователь ведёт NKS — живой граф знаний своей методологической работы. Узлы графа уже продуманы — в словаре пользователя, с его различениями и конвенциями. Ответ из training data на эти темы почти всегда хуже ответа из графа: training data не знает ни языка пользователя, ни его именованных различений. При этом граф — не реальность, а модель в натяжении с реальностью: его показания — свидетельство, требующее чтения, а не готовая истина.

Доступ к NKS — через MCP-тулы `iskron_*` (харнесс может добавить к полному имени префикс своего сервера). Если этих тулов нет в окружении — скилл не применяется, работай как обычно.

## Ориентировка, не узнавание

Скилл сработал по знакомому слову — это узнавание, match паттерна, и его недостаточно. Вход в реалм — **ориентировка**: установление ситуации заново — где я, что здесь живо, что болит, чего от меня ждут. Не переноси сценарий из триггера на реалм, пока не посмотрел на сам реалм.

## Протокол входа

Каноническая нить: **вхождение → опознавание мотивации → фокусировка → углубление**, с сужением на каждом шаге. Перед ней — нулевой шаг выбора реалма.

0. **Реалм.** `iskron_realm(action="list")` — что вообще есть. Обычно реалм ясен из темы, имени или прямого упоминания — тогда пропусти. Неясно и вопрос нетривиален — коротко спроси.

   **Канонический адрес реалма.** Живая поверхность принимает три формы: полную `@owner/slug`, короткий id `rN` (иммутабелен — переживает переименование) и UUID. Любая из трёх достаточно устойчива для записи и передачи — но только **скопированная дословно из `iskron_realm(action="list")`**: никогда не угаданная, не выведенная из имени репо, не перенесённая из чужого листинга. Голый slug без владельца deprecated и двусмыслен между владельцами — не пиши его ни в док, ни в передачу, ни в вызов. На первом orient проверь, что заголовок `REALM:` называет нужный реалм; несовпадение или неожиданно пустой граф — стоп-сигнал: пере-листни и разреши канонический адрес до любой записи.

   **Реалм ≠ репозиторий.** Один реалм часто моделирует целую систему из нескольких репозиториев: каждый репозиторий — холон, его роль разработчика — суб-карта. Работая в репо X и нуждаясь в коде, ролях или вопросах репо Y, не ищи другой реалм — ищи холон Y внутри текущего. Отсутствие реалма с именем репозитория — не пробел, а знак, что ты уже в правильном реалме.

1. **Вхождение**: `iskron_orient(realm=<токен>)` — один вызов, один реалм. До ориентации реалм — неразличённость (無極); orient даёт первое различение (太極): холоны, **ACTIVE BIANHUA** (превращения на ходу — карта того, куда реалм движется; читай её первой), entry/exit-крии, живые вимарши, натяжения. Полных описаний нет — это карта, не стена. Импринт: увиденное первым определяет, куда пойдёшь, — качество ориентации задаёт качество всей сессии. ENTRY-крии в выдаче — диагностика границы реалма («точно ли это питается снаружи?»), не меню навигации.

2. **Опознай свою мотивацию.** Кто ты сейчас в этом реалме: читающий (исследуешь, учишься, отвечаешь на вопрос) или делающий (строишь, чинишь, закрываешь)? Мотивация — не задача, а режим зрения: те же узлы, разные фигуры. Читающему полезны уроки (закрытые вимарши с содержанием), проблематизации, семантические наблюдения; делающему — сиды, риски, открытые samshaya. Из мотивации — фильтр поиска: что читать, что игнорировать.

3. **Фокусировка**: `iskron_search(q=<ключевой термин>, realm=<токен>)` — полнотекст по именам и описаниям: что граф уже знает? Найденное цитируй seq-номерами (`#42`). Keyword находит только слова, которые автор употребил; когда понятие могло быть названо иначе или запрос концептуален (описываешь идею, не известный ярлык) — `iskron_semantic_search(q=<идея фразой>)`, по смыслу. На концептуальных вопросах — семантический поиск по умолчанию, не только после промаха. Мотивация превращается в параметры: один и тот же search — разные глаза.

4. **Углубление**: `iskron_look(node_id=<seq>)` — только узлы, действительно нужные для ответа, два-три на реплику. Рёбра важнее тела: как узел вплетён в остальное. `next`-рёбра с описаниями — прашны, вопросы вперёд: читай их и соотноси со своей ситуацией. Противоречие на дне — повод пройти вверх по breadcrumb (кипение вверх), не чинить локально.

## Когда не входить

- Чисто технические вопросы (код, конфигурация, инфраструктура).
- Вопросы про сам тулинг (Claude, плагины, схема NKS).
- Бытовой разговор, приветствия, уточнения внутри уже сориентированного треда.
- Пользователь явно просит взгляд со стороны («забудь на минуту мои заметки»).

## Коэкзистенция с внешними наборами скиллов

Если установленный workflow-набор требует вызова своих скиллов перед любым ответом, ориентация с ним не конфликтует: чтение реалма — это сбор контекста (memory-fetch), не ответ и не действие реализации. Собственное правило приоритета такого набора (например, superpowers: «инструкции пользователя всегда главнее», с явными инструкциями — CLAUDE.md, GEMINI.md, AGENTS.md, прямые просьбы — в высшем приоритете) само подтверждает ритуал orient-first из AGENTS.md. Сначала ориентируйся, потом следуй процессным скиллам набора.

## Что значит «ответить из графа»

Не пересказ одного узла. Паттерн: цитируй релевантные узлы seq-номерами, но **синтезируй** под конкретный вопрос. Если вопросу нужно то, чего в графе нет, — скажи прямо: «в реалме есть #N про X, но для твоего вопроса этого мало» — и предложи либо открыть samshaya-вимаршу (записанное открытое вопрошание), либо разобрать в чате.

## Когда поиск молчит

**Сначала эскалация, потом вывод.** Промах keyword-поиска — не доказательство молчания реалма: понятие может быть записано другими словами. Переспроси через `iskron_semantic_search`. Только когда молчит и он — реалм темы не покрывает.

И тогда — **не замазывай** пробел ответом из training data. Скажи: «в реалме об этом ничего — отвечаю из общих соображений; откроем samshaya?». Честный пробел ценнее уверенно звучащего промаха.

## Навигация по натяжению

Внутри графа двигайся **по натяжению**, не по ключевым словам. Натяжение — структурное несоответствие, вычислимое из графа: приглашение к расследованию, а не приговор. Самый информативный сосед узла — обычно самый напряжённый, не самый текстуально похожий. `iskron_orient(focus=<seq>)` — без линзы — ставит тебя на узел и показывает окрестность: натяжения, открытые вимарши, соседей; там чаще всего и живёт живое мышление. `iskron_orient` — единый диспетчер чтения связности: **focus** — где стоишь, **lens** — куда смотришь. Без линзы он печатает подсказчик — какие линзы стоит открыть следом (`lens="trace"` — пройти эстафету феномена, `lens="tensions"` — структурное здоровье, `lens="vimarshas"` — поле вопрошания, `lens="bianhua"` — лес превращений) с готовыми аргументами — следуй ему.

В `lens="tensions"` натяжения приходят **сгруппированными по разрешающему жесту**, не плоским списком: **weave** (замкни структуру), **address** (ответь, закрой вопрошание), **reverify** (потрогай, обнови модус), **boundary** (санкционированный край — информация, не работа). Группа говорит, *кто* действует: weave → скилл weaving; address → повестка inquiry/assembly; reverify → фон; boundary → оставь, это край реалма. Каждая поверхность объявляет свой охват (строка `Scope:`, «covers N/M detectors») — дешёвый фильтр `has_tension` и обзор orient покрывают лишь часть; полный реестр — `lens="tensions"`. Читай декларацию: тонкий список — не доказательство чистого реалма.

## Экономия контекста

Граф растёт, контекстное окно — нет. Три опоры: **входи в границу** — иерархическая декомпозиция скрывает нерелевантное, смотри изнутри конкретного холона или крии, а не с этажа реалма; **фильтруй по мотивации** — читай нить, соответствующую задаче, не весь граф; **модусы — встроенный фильтр** — visarjana отпущено, atita прошло. Прагматика: около пяти вызовов NKS на реплику. Сделал семь и не синтезировал — стоп: отвечай из того, что есть, пробел — флагом.

## Семена и карта

Карта первична, семя — остаток. Открытая работа живёт anga-вимаршами на превращениях из `ACTIVE BIANHUA`; сид-вимарша (`genre=hint`) законно несёт только то, чего на карте нет: состояние внешнего мира, выбранный порядок приоритетов, конвенции. Читай сид как указатель: никогда не верь его снапшоту поверх того, что orient и линзы показывают сейчас, и никогда не копируй выдачу линз в сид.

## Гигиена чтения

- Отчитываясь «что открыто / что осталось», исключай волитивные `visarjana` (отпущено) и `virodha` (отвергнуто) — бейдж модуса авторитетнее имени узла. Активные секции orient пре-фильтруют; сырая выдача `iskron_search`/`iskron_look` — нет.
- **Списки orient — витрина с заявленным охватом, не инвентарь.** ROOT KARTAS показывает только корневые роли (суб-роли свёрнуты в «· N sub»); списки крий объявляют остаток «+N quiet». Не выбирай адресата и не заключай «такого узла нет» по витрине — один `iskron_search(q="", node_type=...)` даёт действительное множество.
- Эмодзи на узлах — голос автора; не «чини» их.

## Лёгкая запись как побочный эффект чтения

Если при чтении узлов для ответа замечаешь мелкую правку, которая очевидно верна — недостающая стрелка, устаревшая формулировка, уже исправленная разговором, вимарша, которую пользователь только что разрешил, — можешь применить её сразу: `iskron_update` (узел) или `iskron_arrow(action="link")`, и коротко скажи, что сделал.

**Узел с меткой stale, который ты уже читаешь, — твой: переваривай на месте.** Одна дешёвая проверка — держится ли его утверждение? (условие `attrs.breaks_if`, когда оно есть, делает это одним движением) — и затем: подтверди (тронь эпистемический модус — и назови ступень, против чего проверял: граф или мир), отпусти (`visarjana` с причиной) или поставь сомнение. Не перешагивай через гнилой узел, который заметил: читатель — самый дешёвый агент забвения.

Молча **нельзя**:
- создавать новые феномены и крии;
- открывать вимарши по содержательным конфликтам;
- удалять и переименовывать что бы то ни было;
- перецеплять существующие стрелки.

Всё это — только с явного согласия пользователя.

После inline-правки (`iskron_update` / `iskron_arrow`) — повторный `iskron_look(node_id=...)` тронутого узла: он печатает непройденные проверки в блоке `CHECKS:` — вынеси их пользователю.

## След для пользователя

Ответив из реалма, сделай след видимым: цитируй seq-номера, назови реалм, и — если что-то менял — скажи, что именно. Пользователь — соавтор графа: путь (стрелки в графе) и след (сказанное в разговоре) он должен видеть без инспекции.
